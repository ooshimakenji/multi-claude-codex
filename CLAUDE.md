## Delegação (Claude → Codex)

- Antes de ler >2 arquivos ou um diff grande, **delegar ao Codex** (`codex exec`) em vez de ler eu mesmo.
- Claude planeja, orquestra e revisa. Codex lê e executa.
- Toda delegação declara `modelo / effort / sandbox` **antes** de rodar, e usa `run_in_background`.

Comandos e tabela de modelos: skill `codex`.
